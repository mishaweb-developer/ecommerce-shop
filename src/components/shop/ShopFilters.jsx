import Checkbox from '../ui/Checkbox';import Button from '../ui/Button';
const audiences=['Women','Men','Kids'];const prices=['Under €25','€25–€50','€50–€100','Over €100'];
export default function ShopFilters({categories=[]}){return <aside className="rounded-[10px] bg-surface p-5"><h2 className="title-text mb-6 font-bold">Filters</h2><FilterGroup title="Audience" items={audiences}/><FilterGroup title="Category" items={categories.map(x=>x.name||x)}/><FilterGroup title="Price" items={prices}/><Button variant="outline" size="small" className="w-full">Clear Filters</Button>{/* TASK-03: TODO: Poveži filtere sa URL parametrima search, audience, category, minPrice i maxPrice. HINT: useSearchParams. RULE: Samo jedan cenovni opseg može biti aktivan. */}</aside>}
function FilterGroup({title,items}){return <fieldset className="mb-7"><legend className="mb-3 font-semibold">{title}</legend><div className="flex flex-col gap-3">{items.map(item=><Checkbox key={item} id={`${title}-${item}`} label={item}/>)}</div></fieldset>}
