export default function Input({id,name,type='text',label,value,defaultValue,onChange,placeholder,error,required=false,disabled=false,size='default',autoComplete,className=''}){return <div className={`w-full ${className}`}><label htmlFor={id} className="body-small mb-2 block font-semibold">{label}{required&&<span className="text-accent"> *</span>}</label><input id={id} name={name} type={type} value={value} defaultValue={defaultValue} onChange={onChange} placeholder={placeholder} required={required} disabled={disabled} autoComplete={autoComplete} aria-invalid={Boolean(error)} aria-describedby={error?`${id}-error`:undefined} className={`w-full rounded-[10px] border bg-white px-4 outline-none transition focus:border-foreground disabled:bg-surface ${size==='large'?'h-[55px]':'h-[50px]'} ${error?'border-accent':'border-muted-foreground'}`}/>{error&&<p id={`${id}-error`} className="mt-1 text-sm text-accent">{error}</p>}</div>}
