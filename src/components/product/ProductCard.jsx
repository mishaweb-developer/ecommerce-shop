import { Image as ImageIcon } from 'lucide-react';import Button from '../ui/Button';
export default function ProductCard({product}){return <article className="group"><div className="flex aspect-[4/5] items-center justify-center rounded-[10px] bg-surface"><div className="text-center text-muted-foreground"><ImageIcon className="mx-auto mb-2"/><span className="text-xs">Product image</span></div></div><div className="pt-4"><p className="text-xs uppercase text-muted-foreground">{product.category}</p><h3 className="mt-1 font-semibold">{product.name}</h3><p className="body-small mt-1 font-semibold">€{Number(product.price).toFixed(2)}</p><Button variant="outline" size="small" className="mt-4 w-full" onClick={()=>{}}>Add to Cart</Button></div>{/* TASK-08: TODO: Klik kartice vodi na detalje, a dugme radi quick-add bez navigacije. HINT: Ako sizes sadrži L koristi L, inače prvi size; quantity je 1. */}</article>}
